
const Spotlight = ({size="400px", className}) => (
    <div style={{width: size, height: size}} className={`bg-[#087E5A]/20 rounded-full blur-2xl absolute ${className}`}></div>
)

export default Spotlight
